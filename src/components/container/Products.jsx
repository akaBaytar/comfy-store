import { Fragment } from 'react';

import { Grid, List } from '../../layouts';

const Products = () => {
  return (
    <Fragment>
      <Grid title='featured' />
      <List title='featured' />
    </Fragment>
  );
};

export default Products;
